spring:
  application:
    name: kbo-batch
  profiles:
    active: local
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql

scrap:
  ranking: https://www.koreabaseball.com/Record/TeamRank/TeamRankDaily.aspx